# Copie para .env e preencha antes de rodar o servidor (dev ou produção).
# NUNCA commite o .env de verdade — .gitignore já cobre isso.

# Obrigatório. Segredo usado pra assinar os tokens de login (JWT). Gere um
# valor aleatório longo (ex.: `openssl rand -hex 32`) — trocar esse valor em
# produção desloga todo mundo instantaneamente.
JWT_SECRET=

# Porta que o servidor Express escuta. Em hospedagens gerenciadas (ex.: o
# "Deploy Web App" da Hostinger) a plataforma costuma injetar essa variável
# sozinha — só defina manualmente se estiver rodando num VPS por conta própria.
PORT=4000

# Obrigatórios. Credenciais do banco MySQL gerenciado (ex.: o banco criado
# pelo painel da Hostinger em "Bancos de dados"). Não dá pra usar SQLite em
# produção nessa hospedagem — o diretório do app é recriado do zero a cada
# deploy, então um arquivo local se perderia a cada atualização do site.
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=

# Opcional. Restringe o CORS da API a uma origem específica. Sem essa
# variável, a API aceita requisições de qualquer origem (aberto) — como o
# front roda no mesmo domínio da API em produção, isso raramente importa na
# prática, mas dá pra travar se quiser bloquear scraping de outro domínio.
# ALLOWED_ORIGIN=https://seusite.com

# Opcional, mas é o que faz o multiplayer funcionar em 4G e em Wi-Fi de
# faculdade/empresa. Sem isso o jogo só conecta quem está numa rede
# permissiva: WebRTC precisa de um servidor TURN pra RETRANSMITIR o tráfego
# quando não existe caminho direto (celular atrás de CGNAT, rede que bloqueia
# UDP). Antes o app dependia do TURN gratuito do PeerJS, que foi descontinuado
# — os hostnames saíram do DNS e o jogo ficou sem relay nenhum.
#
# Caminho recomendado — Cloudflare Realtime TURN (1.000 GB/mês grátis, e tem
# TURN sobre TLS na 443, que é o único que passa em rede de faculdade):
#   1. painel da Cloudflare -> Realtime -> TURN -> criar uma TURN key
#   2. copie o Token ID e o API Token pra cá
# O servidor usa esses dois pra emitir credenciais curtas pro navegador; o
# segredo em si nunca sai daqui.
# TURN_KEY_ID=
# TURN_KEY_API_TOKEN=

# Alternativa: um coturn próprio com credencial fixa. Aceita vários endereços
# separados por vírgula — inclua o de TCP/443, que é o que atravessa firewall
# que só libera tráfego web.
# TURN_URL=turn:SEU-HOST:3478,turns:SEU-HOST:443?transport=tcp
# TURN_USERNAME=
# TURN_CREDENTIAL=

# Opcional. Chaves VAPID pra notificação push do navegador (avisa quem tem
# conta quando sai novidade, mesmo com o jogo fechado). Sem as 3 variáveis
# preenchidas, o recurso fica desligado sozinho (GET /api/notify/vapid-public-key
# devolve null e o botão de ativar some no front) — não derruba o servidor.
# Gere o par com: node -e "console.log(require('web-push').generateVAPIDKeys())"
# (rode dentro de server/, com as deps já instaladas). VAPID_SUBJECT é um
# "mailto:" de contato — o padrão do protocolo Web Push, pra quem opera o
# Push Service (Google/Mozilla/etc.) poder avisar o dono se algo sair errado.
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:contato@brasileiraolendario.com.br

# Opcional. Envio de email de novidade via Resend (https://resend.com) — API
# HTTP simples (sem SMTP, que a Hostinger costuma bloquear na porta de
# saída). Precisa de um domínio verificado no painel da Resend; EMAIL_FROM
# tem que usar esse domínio (ex.: "Brasileirão Lendário <novidades@seudominio.com.br>").
# Sem as duas variáveis, o envio de email fica desligado sozinho.
# RESEND_API_KEY=
# EMAIL_FROM=Brasileirão Lendário <novidades@brasileiraolendario.com.br>

# Obrigatório só pra usar POST /api/admin/notify-news (dispara push + email
# de uma novidade pra toda a base) — um segredo qualquer, comprido e
# aleatório (ex.: `openssl rand -hex 32`), enviado no header x-admin-secret.
# Sem essa variável, o endpoint fica 503 pra todo mundo (nem aceita tentar).
# ADMIN_SECRET=
